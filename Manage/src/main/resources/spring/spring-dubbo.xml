<?xml version="1.0" encoding="UTF-8"?>
<!-- 添加 DUBBO SCHEMA -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 应用名 -->
    <dubbo:application name="consumer-manage"/>
    <!-- 连接到哪个注册中心 -->
    <dubbo:registry address="${DUBBO.REGISTRY.ADDRESS}"/>
    <!-- 消费方用什么协议获取服务（用dubbo协议在20880端口暴露服务） -->
    <dubbo:protocol port="20880" />


    <!-- 调用的接口 -->
    <dubbo:reference id="loginService" interface="com.practice.service.LoginService"/>


    <!--<dubbo:reference id="systemService" interface="com.upingwang.core.service.SystemService"/>-->
    <!--<dubbo:reference id="redisService" interface="com.upingwang.core.service.RedisService"/>-->
    <!--<dubbo:reference id="schoolService" interface="com.upingwang.core.service.SchoolService"/>-->
    <!--<dubbo:reference id="imgUploadService" interface="com.upingwang.core.service.ImgUploadService"/>-->


</beans>